{% extends 'base.html.twig' %}

{% block title %}Detail - {{ parent() }}{% endblock %}

{% block main %}
    <h1>{{ serie.name }}</h1>
    <p>{{ serie.overview }}</p>
    <p>note : {{ serie.vote }}</p>
{% endblock %}